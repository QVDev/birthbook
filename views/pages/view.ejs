<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
    <% include ../partials/analytics %>
  </head>
  <body>
    <header>      
      <% include ../partials/welcome %>
      <h1>
        Collect birthdays from friends and family with ease.
      </h1>
    </header>
    <main>
      <br>
      <br>
      <%for (var result in data){%>
        <%if (result != "recoverId" && result != "id"){%>
            <a href="https://calendar.google.com/calendar/render?action=TEMPLATE&text=Birthday of <%=result%>&dates=<%=data[result].date.replace(/-/g, '');%>/<%=data[result].date.replace(/-/g, '');%>&details=Birthday of <%=result%>&trp=false&recur=RRULE:FREQ=YEARLY" target="_blank">Add <%=result%>'s Birthday to Calendar</a>
            <br>
        <%}%>
      <%}%>
      <br>
    </main>
    <% include ../partials/social %>
  </body>
</html>
