<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
    <% include ../partials/analytics %>
    <script src="/quicklink.js" defer></script>
  </head>
  <body>
    <header>      
      <% include ../partials/welcome %>
      <h3>
        Save this link        
        <br>
        <%= recoverLink %>
      </h3>  
      <h4>
        keep it private, for your eyes only<br>          
      </h4>             
    </header>
    <main>      
      <h2>
        Share via:
      </h2>
      <h3>
        <%- include('../partials/social', {text:"Let me know your birthday, and I wont forget 😇"}); %>
        <br>
        <br>
        or copy url
        <br>
        <a href="<%= shareLink %>" target="_blank"><%= shareLink %></a>        
      </h3>
      <br>
      <h2>
        Birthdays:
      </h2>      
      <%for (var result in data){%>
        <%if (result != "recoverId" && result != "id" && result != "quicklink"){%>
            <a href="https://calendar.google.com/calendar/render?action=TEMPLATE&text=Birthday of <%=result%>&dates=<%=data[result].date.replace(/-/g, '');%>/<%=data[result].date.replace(/-/g, '');%>&details=Birthday of <%=result%>&trp=false&recur=RRULE:FREQ=YEARLY" target="_blank"><%=result%>'s Birthday @<%=data[result].date%></a>
            <br>
        <%}%>
      <%}%>
      <br>
      <form id="create_quicklink">
        <h3>
          Shorten your private url here:
        </h3>
        <input name="quicklink" type="text" maxlength="100" required placeholder="Enter short url..." aria-labelledby="submit-dream">
        <input name="recoverId" type="hidden" value="<%= recoverId %>"  aria-labelledby="submit-dream">
        <input name="id" type="hidden" value="<%= bdayId %>" aria-labelledby="submit-dream">
        <button type="submit" id="submit-dream">Shorten url</button>
      </form>
    </main>
    <footer>
      <%- include('../partials/social', {shareLink: 'http://birthbook.me', text:"Never forget birthdays again.."}); %>
    </footer>
  </body>
</html>
