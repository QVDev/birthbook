<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
    <% include ../partials/analytics %>
  </head>
  <body>    
    <header>
      <% include ../partials/welcome %>
      <h1>
        Collect birthdays from friends and family with ease.
      </h1>
    </header>
    <main>
<!--       List for <%= data.email %> & <%= data.id %> -->
      <br>
      <br>
      <%for (var result in data){%>
        <%if (result != "email" && result != "id"){%>
            <a href="https://calendar.google.com/calendar/render?action=TEMPLATE&text=Birthday of <%=result%>&dates=<%=data[result].date.replace(/-/g, '');%>/<%=data[result].date.replace(/-/g, '');%>&details=Birthday of <%=result%>&trp=false&recur=RRULE:FREQ=YEARLY">Add <%=result%>'s Birthday to Calendar</a>
            <br>
<!--           <%=result%> <%=data[result].date.replace(/-/g, '');%><br> -->
        <%}%>
      <%}%>
      
<!--       <a href="https://calendar.google.com/calendar/render?action=TEMPLATE&text=Event asdName&dates=20170228/20170228&details=Describe your event.asd&location=Evenasdt Location&trp=true">Link</a> -->
    </main>
    <% include ../partials/social %>
  </body>
</html>
